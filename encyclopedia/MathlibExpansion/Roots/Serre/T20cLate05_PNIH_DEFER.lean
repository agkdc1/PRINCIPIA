import Mathlib.NumberTheory.Padics.PadicNumbers

/-!
# T20c_late_05 PNIH_DEFER — p-adic numbers, integers, Hensel (defer)

**Classification.** `defer` / Chapter II. Covered upstream by
`Mathlib.NumberTheory.Padics.{PadicNumbers,PadicIntegers,Hensel}`:
`ℚ_p`, `ℤ_p`, DVR structure, residue field `F_p`, Hensel's lemma.

**Citation.** Serre, *A Course in Arithmetic*, Ch. II §1–2.
Historical parent: Hensel, *Theorie der algebraischen Zahlen* (1908).
-/

namespace MathlibExpansion
namespace Roots
namespace Serre
namespace T20cLate05_PNIH

/-- **PNIH_01** citation marker. `ℚ_p`, `ℤ_p`, DVR structure with maximal
ideal `pℤ_p`, residue field `ℤ_p/pℤ_p ≅ F_p`, Hensel's lemma for simple
root lifting, all upstream.
Citation: Serre Ch. II §1–2. -/
axiom padic_numbers_hensel_marker : True

end T20cLate05_PNIH
end Serre
end Roots
end MathlibExpansion
