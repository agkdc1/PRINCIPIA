import Mathlib.CategoryTheory.Sites.Sheaf

/-! # T20c_late_07 CCSS — Classical Comparison for Smooth Schemes (DEFER, SGA 4 Exp. XI)

    **Classification.** `defer` — real theorem, but opens an analytification /
    classical-cohomology side campaign off the SGA 4 first-wave critical path.
    Citation-backed marker only; not a first-wave breach.
    **Citation.** Grothendieck–Verdier, *SGA 4* Exp. XI (Artin, *Comparaison avec la
    cohomologie classique: cas d'un schéma lisse*), §XI.4 (comparison isomorphism
    `H^q(X_ét, Z/nZ) ≅ H^q(X(ℂ)_an, Z/nZ)` for smooth `X/ℂ`).
    Historical parent: Artin, *Topologie d'une variété algébrique complexe* (CIME 1965). -/
namespace MathlibExpansion
namespace Roots
namespace Grothendieck
namespace T20cLate07_CCSS_DEFER

/-- **CCSS_DEFER** classical-comparison citation anchor. Full Exp. XI analytification
    + étale-vs-classical isomorphism for smooth complex schemes is deferred to a
    separate analytic side campaign (not SGA 4 core breach). -/
axiom classical_comparison_smooth_scheme_citation_marker : True

end T20cLate07_CCSS_DEFER
end Grothendieck
end Roots
end MathlibExpansion
