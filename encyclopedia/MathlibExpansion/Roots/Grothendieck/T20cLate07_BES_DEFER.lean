import Mathlib.CategoryTheory.Sites.Sheaf

/-! # T20c_late_07 BES — Big Etale Site of Schemes (DEFER, SGA 4 Exp. VII boundary)

    **Classification.** `defer` — already-present upstream; cite as fact, warn against
    big-site shortcut. No standalone Lean breach needed.
    **Citation.** Grothendieck–Verdier, *SGA 4* Exp. VII (Grothendieck, *Site et topos étale
    d'un schéma*), §VII.1 (big étale site), §VII.2 (fonctorialité). -/
namespace MathlibExpansion
namespace Roots
namespace Grothendieck
namespace T20cLate07_BES_DEFER

/-- **BES_DEFER** big-étale-site citation anchor. Small-étale site (SET) is the real
    geometric breach target; big-étale site is a citation fact. -/
axiom big_etale_site_citation_marker : True

end T20cLate07_BES_DEFER
end Grothendieck
end Roots
end MathlibExpansion
