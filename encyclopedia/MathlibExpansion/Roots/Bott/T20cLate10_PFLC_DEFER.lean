import Mathlib.CategoryTheory.Sites.Sheaf

/-! # T20c_late_10 PFLC_DEFER — Path Fibration & Loop Space (Bott–Tu 1982 §III.17 appendix, defer, DEFER)
    **Classification.** defer — path-space fibration `ΩX → PX → X` is a companion construction
    to the Serre SS; touched only tangentially in Bott–Tu and not needed for the primary
    characteristic-class thread. Single axiom.
    **Citation.** Bott–Tu §III.17 (path-space fibration aside); Serre 1951 *Homologie singulière*
    — §II.7 path-loop fibration; Hu 1959 *Homotopy Theory*. -/
namespace MathlibExpansion
namespace Roots
namespace Bott
namespace T20cLate10_PFLC_DEFER

/-- **PFLC_01** (deferred) path-space fibration `ΩX → PX → X` with contractible total space `PX`;
    Serre SS computes `H^*(ΩX)` from `H^*(X)` (Bott–Tu §III.17 aside; Serre 1951). -/
axiom pflc_path_loop_fibration_deferred_marker : True

end T20cLate10_PFLC_DEFER
end Bott
end Roots
end MathlibExpansion
