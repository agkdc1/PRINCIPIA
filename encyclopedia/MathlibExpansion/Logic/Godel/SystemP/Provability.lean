import MathlibExpansion.Logic.Godel.SystemP.Derivability

/-!
# Compatibility alias for system-`P` provability

Several recon reports refer to a `SystemP/Provability.lean` surface. The live
owner of `ProvableFrom` is `Derivability.lean`; this file keeps the import path
stable for downstream consumers.
-/
