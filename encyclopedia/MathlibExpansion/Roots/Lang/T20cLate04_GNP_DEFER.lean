import Mathlib.NumberTheory.NumberField.Basic

/-!
# T20c_late_04 GNP — Geometry of numbers & parallelotopes (DEFER)

**Classification.** `defer`. Chapter 5 Minkowski geometry-of-numbers substrate
fully reusable upstream (Mathlib); cite into ideal-counting and class-number
consumers rather than rebuild.

**Citation.** Lang, *Algebraic Number Theory*, GTM 110 (1970), Ch. 5.
Historical parent: Minkowski, *Geometrie der Zahlen* (1896).
-/

namespace MathlibExpansion
namespace Roots
namespace Lang
namespace T20cLate04_GNP_DEFER

/-- **GNP_DEFER** Minkowski lattice-point facade. Deferred citation to
upstream Mathlib Minkowski theorem for ideal-counting consumers.
Citation: Lang Ch. 5 §§1-4; Minkowski (1896). -/
axiom gnp_minkowski_marker : True

end T20cLate04_GNP_DEFER
end Lang
end Roots
end MathlibExpansion
